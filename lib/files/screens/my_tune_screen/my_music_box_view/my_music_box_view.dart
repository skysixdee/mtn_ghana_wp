import 'package:flutter/material.dart';

class MyMusicBoxView extends StatelessWidget {
  const MyMusicBoxView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      color: Colors.amberAccent,
    );
  }
}
